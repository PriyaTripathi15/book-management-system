# Base API URL (without trailing slash). Example:
# https://your-project.mockapi.io/api/v1
VITE_API_BASE_URL=https://682c7fb6d29df7a95be3b571.mockapi.io/api/v1

# Optional API key for providers that require one.
# Leave blank for MockAPI/JSON Server unless your provider needs it.
VITE_API_KEY=
